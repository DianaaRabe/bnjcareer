{
  "name": "skills-maker",
  "private": true,
  "version": "1.0.0",
  "description": "Skills Maker — refonte stack découplée (Express + Apollo GraphQL + Prisma / Vite + React)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/api",
    "build": "npm run codegen -w apps/api && npm run db:generate -w apps/api && npm run build -w apps/api",
    "start": "npm run db:migrate:deploy -w apps/api && npm run start -w apps/api",
    "lint": "npm run lint -w apps/api",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:migrate:deploy": "npm run db:migrate:deploy -w apps/api",
    "db:seed": "npm run db:seed -w apps/api",
    "codegen": "npm run codegen -w apps/web"
  },
  "engines": {
    "node": ">=18"
  }
}
